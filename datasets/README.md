# HoloSphere Vector Datasets

This directory contains real-world vector datasets in standard `.fvecs` format for auditing HoloSphere retrieval contracts.

Formats supported:
- `.fvecs`: 4-byte little-endian integer dimension followed by `dim` 32-bit floats per vector.
